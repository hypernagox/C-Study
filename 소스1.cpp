//#include "CppStudyClient.h"
#include <iostream>
#include <fstream>
#include <numeric>
#include <string>
#include <ranges>
#include <algorithm>
#include <random>
#include "String.h"
#include <map>
using namespace std;


//// [문제] foo는 주소와 배열의 크기를 인자로 받아서 동적배열을 0~크기-1 로 초기화 하는 함수이다.
//// 해당 함수를 선언하고 정의하라.
//
//int main()
//{
//	int* p;
//
//	int n = 5;
//
//	foo(p,n);
//
//	for (int i = 0; i < n; ++i)
//	{
//		cout << p[i] << endl;
//	}
//
//	
//}

// [문제] qsort에서 함수 포인터로 넘긴 함수의 호출 횟수를 세어라.

// [문제] 제시된 bsort를 이용해 구조체를 x좌표 기준으로 정렬하라

// [문제] 파일에는 구조체의 갯수와 이후 각 좌표가있다.
// 파일 맨위에 적혀진 원소의 갯수를 이용하여 해당 갯수만큼 메모리를 동적할당하고 이후 파일의 정보들로 구조체의 좌표값을 채운다
// 이후 qsort를 이용해서 원점과의 거리순으로 내림차순 정렬해라.




